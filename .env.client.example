# Client-safe Flutter config only.
# Copy to .env.client and fill these values for plain `flutter run`.
# Do not put OpenAI, service-role, Firebase admin, or database passwords here.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=
